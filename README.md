# Material UI - Test task

***Get project be runned***
***
To build the project, install it and run:

```bash
npm install --legacy-peer-deps
npm run dev
```
***
You should also get apikey of [OMDB Api](https://www.omdbapi.com/) and setup .env file
```bash
cp .env.example .env
```
Put your Api key to variable for fluent work of this app

And here you are